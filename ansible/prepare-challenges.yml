---
- hosts: webservers
  become: yes
  tasks:
    - name: Ensure ACME challenge directory exists
      file:
        path: /var/www/html/.well-known/acme-challenge
        state: directory
        owner: www-data
        group: www-data
        mode: '0755'
